# 実装タスクリスト:週間時間割・生徒メモ・所感自動生成ツール

`docs/design.md`(技術設計)と `docs/design/components.md`(共通コンポーネント)をもとにしたタスク一覧。順序: セットアップ → 基盤 → デザインシステム実装 → 共通コンポーネント → 画面 → 仕上げ。各タスクは概ね1〜2時間で完了する粒度。

## セットアップ

- [x] **T-001** Next.js (App Router) + TypeScriptプロジェクトを初期化する
  - DoD: `npm run dev` でトップページが表示される
- [x] **T-002** Tailwind CSSを導入する
  - DoD: `docs/design/design-system.md` のトークンを反映する土台として、Tailwind設定ファイルが存在し、任意のクラスがページに反映される
- [x] **T-003** ESLint / Prettierを設定する
  - DoD: `npm run lint` が実行でき、意図的な違反が検出される(既定重大度のためwarning表示。エラー扱いへの格上げはスコープ外と判断)
- [x] **T-004** Supabaseプロジェクトを作成し、Supabase CLIをローカルに連携する
  - DoD: `supabase start` でローカルスタックが起動し、Studioにアクセスできる
  - 注記: 今回はローカルスタックの連携のみ実施。クラウド側のSupabaseプロジェクト作成・`supabase link`は未実施(要ダッシュボード操作のため別途)
- [ ] **T-005** Vercelへのデプロイ設定を行う
  - DoD: mainブランチの変更が自動デプロイされ、疎通確認(トップページ表示)ができる
  - 注記(2026-08-17): 当初Cloudflare Pagesを対象にしていたが、T-079で判明したNext.js 16の`proxy.ts`(Node.jsランタイム固定)と`@opennextjs/cloudflare`アダプタの非互換ブロッカーが解消の見込みが立たなかったため、Vercelへ変更した。Vercelはmiddleware/proxyのNode.jsランタイムをネイティブサポートしているため、このブロッカー自体が発生しない。あわせて、Hono側で秘密鍵取得にCloudflare Workers専用API(`getCloudflareContext()`)を使っていた箇所を`process.env`ベースに修正済み(`lib/hono-server/routes/ai.ts`)。Cloudflare関連の設定ファイル(`wrangler.jsonc`, `open-next.config.ts`, `lib/hono-server/cloudflare-env.secrets.d.ts`)と`package.json`のCloudflare専用scripts/devDependenciesは現時点では未整理(削除しても実害はないが、Vercelデプロイ自体の可否には影響しないため後回し)。GitHub連携・Vercelアカウントでの自動デプロイ設定は未実施(要ダッシュボード操作のため別途)。DoD未達のため未チェック
- [x] **T-006** Vitest・React Testing Library・Playwrightをセットアップする
  - DoD: それぞれのサンプルテストが1本ずつ通る

## 基盤

- [x] **T-007** `teacher_profile` テーブルと `auth.users` 作成時トリガーを実装する(`docs/design.md` §4.2)
  - DoD: サインアップ直後に `teacher_profile` 行が自動作成されることをローカルDBで確認できる
- [x] **T-008** `class` / `class_number_counter` テーブルを作成しRLSを設定する
  - DoD: 他教員IDでの `class` へのアクセスがRLSで拒否されることを確認できる
- [x] **T-009** `student` テーブルを作成しRLSを設定する(クラス経由のポリシー)
  - DoD: 他教員のクラスに紐づく生徒へのアクセスがRLSで拒否される
- [x] **T-010** `subject` テーブルを作成しRLSを設定する
  - DoD: 科目名の重複が許可され、DBユニーク制約がないことを確認できる(`docs/design.md` §0)
- [x] **T-011** `timetable_master_slot` テーブルを作成しRLSを設定する
  - DoD: `(teacher_id, weekday, period)` のユニーク制約が機能する
- [x] **T-012** `weekly_subject_override` / `weekly_class_override` テーブルを作成しRLSを設定する
  - DoD: 同一マス・同一週での重複が制約で防がれる
- [x] **T-013** `memo` テーブルを作成しRLSを設定する
  - DoD: `(student_id, subject_id, note_date, period)` のユニーク制約でupsertが機能する
- [x] **T-014** `student_comment` テーブルを作成しRLSを設定する
  - DoD: `(student_id, period_start_date, period_end_date)` のユニーク制約でupsertが機能する
- [x] **T-015** `ai_provider_setting` / `prompt_template` テーブルを作成しRLSを設定する
  - DoD: APIキーが平文で保存されないカラム構成になっている(暗号化は T-028 で実装)
- [x] **T-016** `create_class` / `update_class_grade` / `delete_class` のRPC関数を実装する(`docs/design.md` §4.4)
  - DoD: 組番号採番、学年変更時の再採番、生徒が存在する場合の削除拒否がそれぞれDB結合テストで再現できる
- [x] **T-017** `delete_subject` RPC関数を実装する
  - DoD: メモで使用中の科目削除が拒否され、時間割のみ使用中の場合は削除後に未設定へ戻ることを確認できる
- [x] **T-018** `import_students` RPC関数を実装する
  - DoD: 必須項目欠落・出席番号重複の行がエラーとして返り、有効な行だけが登録される(F2)
- [x] **T-019** `save_timetable_master` RPC関数を実装する
  - DoD: マスタ保存後、内容が一致した個別変更が自動的に削除される巻き戻しロジックが動作する(F4)
- [x] **T-020** `update_timetable_start_date` RPC関数を実装する
  - DoD: メモが1件以上ある場合、`force=false` では拒否され、`force=true` では更新できる(F4)
- [x] **T-021** `export_teacher_data` RPC関数を実装する
  - DoD: 生徒ごとにメモ・所感がまとまったJSONが返り、`ai_provider_setting` が含まれない(F14)
- [x] **T-022** `supabase-browser.ts` / `supabase-server.ts` クライアントを実装する
  - DoD: ブラウザ側・Server Component/middleware側それぞれからセッションを取得できる
- [x] **T-023** `proxy.ts`(Next.js 16。旧`middleware.ts`)で未ログイン時のリダイレクトを実装する(F8)
  - DoD: 未ログイン状態で `(main)` 配下にアクセスするとログイン画面にリダイレクトされる
- [x] **T-024** TanStack Query Providerを設定する
  - DoD: 任意のクエリフックがキャッシュされ、DevToolsで確認できる
- [x] **T-024b** 共通クラス選択フック `useClassOptions()` を実装する
  - DoD: 教員のクラス一覧取得+選択中クラスの生徒一覧取得をまとめて提供する。生徒名簿(T-055)・生徒別メモ一覧(T-064)・所感画面(T-065)の3箇所から共通で呼び出され、各画面で同じロジックを再実装しない
- [x] **T-025** `shared/schemas` にzodスキーマを実装する
  - DoD: `memoInputSchema` 等がフロントのバリデーションで使用できる
- [x] **T-026** `shared/` の純粋関数(`pseudonym.ts`, `week.ts`, `prompt-builder.ts`, `resolve-weekly-slots.ts`)を実装する
  - DoD: それぞれ単体テストが書け、仮名コード生成例(F12)・週番号計算が期待通りの値を返す
- [x] **T-027** Honoアプリの基盤とCSPヘッダーを実装する(`docs/design.md` §1, §5.1)
  - DoD: `app/api/[[...route]]/route.ts` がマウントされ、レスポンスに `default-src 'self'` を含むCSPヘッダーが付与される
- [x] **T-028** `crypto.ts`(APIキー暗号化・復号)を実装する
  - DoD: AES-256-GCM・AAD=teacher_idで暗号化/復号の往復が単体テストで確認できる
- [x] **T-029** AIアダプタ(openai / anthropic / gemini)を実装する
  - DoD: 各アダプタがモック環境で `generateComment` を呼び出せる
- [x] **T-030** `shared/ai-models.ts` の許可モデルリストを確定・実装する(未決事項の解消)
  - DoD: 3プロバイダそれぞれに既定モデルと選択肢が定義され、許可リスト外のモデル指定がHono側で400になる

## デザインシステム実装

- [x] **T-031** `docs/design/design-system.md` のトークン(グレースケール・状態色・スペーシング・ボーダー半径・シャドウ・ブレークポイント)をTailwind設定/CSS変数に反映する
  - DoD: 任意のコンポーネントからトークン名でスタイルを参照できる
- [x] **T-032** グローバルベーススタイル(reset、フォント指定、基本レイアウト)を実装する
  - DoD: `design-system.md` のフォントファミリー・サイズスケールがページ全体に適用される

## 共通コンポーネント実装

`docs/design/components.md` の各コンポーネントに対応。

- [x] **T-033** Button(Primary/Secondary/Outline/Danger、サイズ、loading状態)
- [x] **T-034** Input(text/email/password/number/date/textarea、error状態)
- [x] **T-035** Select(通常セレクト+セグメントコントロール、empty状態)
- [x] **T-036** Toggle(共有区分用)
- [x] **T-037** Table/List(表形式+カードリスト形式、empty/loading状態)
- [x] **T-038** Card(メモカード/所感カード共通)
- [x] **T-039** Modal + ConfirmDialog(フォームモーダル、確認ダイアログ)
- [x] **T-040** InlineMessage(info/warning、常時表示)
- [x] **T-041** Toast(success/error)
- [x] **T-042** EmptyState(案内文+アクションボタン)
- [x] **T-043** LoadingSpinner / Skeleton
- [x] **T-044** Badge/Tag(共有区分・作成方法・「変更あり」)
- [x] **T-045** PasteOrUploadArea(CSV取り込み欄)
- [x] **T-046** CodeBadge(仮名コード表示)
- [x] **T-047** Header/GlobalNav(ナビゲーション、ログアウト。グローバルなクラス切替は持たない)
  - DoD: 画面全体の高さに固定されたサイドバーとして実装され、メインコンテンツを大きくスクロールしてもサイドバーの表示位置・内容は変化しない(サイドバーとメインが独立にスクロールする)
- [x] **T-048** TimetableGrid(セル描画の共通基盤。編集モード切替は画面側で分岐)

各タスクのDoD共通: Storybook等での確認、または該当画面への組み込みで `default/hover/active/disabled/loading` 等 `components.md` に記載の状態が目視確認できる。

## 画面実装

- [x] **T-049** ログイン画面(F8)
  - DoD: 正常ログイン、認証失敗時のエラー表示が動作する
- [x] **T-050** サインアップ画面(F8)
  - DoD: 新規登録成功、登録済みメールアドレスでのエラー表示が動作する
- [x] **T-051** パスワードリセット申請画面(F8)
  - DoD: 登録有無にかかわらず同一メッセージが表示される
- [x] **T-052** パスワードリセット実行画面(F8)
  - DoD: 新しいパスワードで再ログインできる
- [x] **T-053** クラス管理画面(一覧・作成)(F1)
  - DoD: 通常学年・「特支」いずれでもクラスが作成され、組番号が自動採番される
- [x] **T-054** クラス管理画面(編集・削除・確認ダイアログ)(F1)
  - DoD: 学年変更時の確認ダイアログ、生徒がいるクラスの削除拒否、時間割使用中クラスの削除確認が動作する
- [x] **T-055** 生徒名簿画面(クラス選択・クラス0件時の空状態)(F2)
  - DoD: 画面内のクラス選択でクラスを切り替えると、そのクラスの生徒一覧・インポート対象が切り替わる(`useClassOptions()`、T-024bを利用)。クラスが1件も登録されていない場合は一覧・インポート欄を表示せず、クラス管理画面への導線を表示する
- [x] **T-055b** 生徒名簿画面(CSV/貼り付けインポート)(F2)
  - DoD: 必須項目欠落・出席番号重複行がエラー一覧として表示される
- [x] **T-056** 生徒名簿画面(一覧・編集・削除・仮名コード表示)(F2)
  - DoD: 生徒削除時に「メモ・所感も完全削除される」確認ダイアログが表示され、削除が反映される。生徒一覧の氏名部分をクリックすると、その生徒を選択済みの状態で生徒別メモ一覧画面に遷移する
  - 注記: 氏名リンクは`/memos/students/[id]`(design.mdのルーティング)へのリンクとして実装済み。遷移先の生徒別メモ一覧画面自体はT-064で実装するため、実際に正しい画面が表示されることの確認はT-064実装時に行う
- [x] **T-057** 科目管理画面(F3)
  - DoD: 使用中科目の削除がエラーになり、未使用科目は削除できる
- [x] **T-058** 時間割マスタ設定画面・一括モード(F4)
  - DoD: クラス選択+マスごとの科目選択で全マスタが保存される
- [x] **T-059** 時間割マスタ設定画面・教科担任制モード(F4)
  - DoD: マスごとに異なる科目・クラスが個別に保存され、モード切替では既存データが変化しない
- [x] **T-060** 時間割マスタ設定画面・起算日設定/年度更新(F4)
  - DoD: メモ保存後は起算日が読み取り専用になり、「年度を更新する」経由でのみ再設定できる
- [x] **T-061** 週次時間割画面・表示/週送り(F5)
  - DoD: 起算日から計算した週番号・日付範囲が表示され、週指定でジャンプできる
- [x] **T-062** 週次時間割画面・マス編集モーダル/個別変更(F5)
  - DoD: 科目・クラスの個別変更保存、「マスタの内容に戻す」、「この授業を記録する」遷移が動作する
  - 注記: 「この授業を記録する」は`/memos/record?date=...&period=...&from=/timetable/weekly`への遷移として実装済み。T-063実装後にE2Eで遷移先画面の表示・「戻る」導線まで確認済み
- [x] **T-063** 授業記録画面(F6)
  - DoD: 生徒一覧が氏名のみの折りたたみ行で表示され、タップで展開してメモ入力・共有区分トグル・個別保存ボタンが操作できる。生徒ごとの個別保存、既存メモがある生徒への「入力済み」インジケーター表示、共有時の注意文表示、科目・クラス未設定時の案内表示が動作する
  - 注記: 「戻る」導線は遷移元を`from`クエリパラメータで受け取る簡易的な仕組みとして実装(design.mdに実装パターンの明記なし)。週次時間割画面・生徒名簿画面からの遷移では`from`を付与済み。要件定義書F7は「授業記録画面から生徒別メモ一覧画面に遷移した場合の戻る」にも言及しているが、screens.mdの授業記録画面の構成要素には生徒別メモ一覧への遷移導線が明記されていないため、本実装では追加していない(ドキュメント間の未解消の食い違いとして記録)
- [x] **T-064** 生徒別メモ一覧画面(F7)
  - DoD: 画面内のクラス選択で生徒選択の候補が絞り込まれる(`useClassOptions()`、T-024bを利用)。日付順/教科別表示切替、編集・削除・共有区分切替が動作する
  - 追記(2026-08-17): ナビゲーションメニューにも項目を追加し、`/memos/students`(IDなし)で直接開けるようにした。ルートを`students/[id]`から`students/[[...id]]`(オプショナルキャッチオール)に変更し、IDがない場合はクラス・生徒とも未選択(先頭のクラスのみ既定選択)から始まる。生徒名簿からの遷移時のみ表示される「戻る」導線は、メニューからの直接遷移時は表示されない
- [x] **T-065** 所感画面の骨格(タブ切替・共通クラス/生徒選択)(F9, F10, F11)
  - DoD: 「生成」「履歴」の2タブを持つ1画面として実装され、画面内のクラス選択・生徒選択がタブ間で共有される(タブを切り替えても選択状態が保持される)。`useClassOptions()`(T-024b)を利用する
  - 注記: 画面への入口として、生徒名簿画面の各行に所感アイコンリンク(`/comments/students/[id]`、生徒別メモ一覧のリンクと同じ「[id]は初期選択のヒント」パターン)を追加した。screens.md/user-flow.mdには本画面への遷移導線が明記されていなかったため、design.mdのルーティング定義([id]付きの単一ルート)から妥当な入口として実装時に補った。生成・履歴タブの中身は準備中のプレースホルダーで、実装はT-066a/T-066b/T-067で行う
  - **2026-08-17追記**: 本タスクで実装した生徒単位・タブ切替の構成は、T-080でクラス単位の一覧編集画面(所感管理画面)に再設計され、置き換えられた。以下T-066a/T-066b/T-067の実装内容も同様
- [x] **T-066a** 所感画面・生成タブ:直接呼び出し(F9, F12)
  - DoD: 期間・目安文字数指定で生成され、AIの応答テキストがそのまま画面に表示される(仮名コード検出・実名置換は行わない)。認証/レート制限エラー時は所感が保存されない
  - 注記: F11(上書き確認ダイアログ)は本来T-067の担当だが、「生成」タブ自身に保存ボタンを持つ(screens.mdの構成要素)ため、生成タブを実際に使える状態にするにはF11の保存ロジック(`student_comment`のupsert+既存チェック)が同時に必要と判断し、本タスクで実装した。T-067では履歴タブ一覧表示・手動作成フォームを追加する(保存ロジックは本タスクのものを再利用)。動作確認は開発用ダミーAPIキーで実際にOpenAIへ本物のHTTPリクエストを送り、暗号化保存→復号→401エラー正規化までの実経路を手動検証したうえで、design.mdの方針通りE2Eは`page.route()`によるモックで生成成功・上書き確認・認証エラー・空状態・APIキー未設定の5ケースを自動化した(`e2e/comments-generate.spec.ts`)
- [x] **T-066b** 所感画面・生成タブ:プロンプトコピー運用(F10, F12)
  - DoD: プロンプトのコピー、外部AIの応答を貼り付け欄に入力してそのまま所感として保存できる(仮名コード検出・実名置換は行わない)
  - 注記: F12の「初めて実行する際の注意文」はAPIキー設定済み(直接呼び出し)・未設定(プロンプトコピー)のどちらの分岐でも常時表示する形に簡略化した(「初回のみ」を判定する永続化の仕組みは設計されていないため)
- [x] **T-067** 所感画面・履歴タブ(F11)
  - DoD: 期間別一覧、上書き確認ダイアログ、手動作成が動作する(クラス・生徒選択はT-065のタブ共通stateを使う)
  - 注記: 既存所感の編集時は期間(開始日・終了日)を読み取り専用にし、作成方法(direct_ai/prompt_copy/manual)は編集前の値を保持したまま内容のみ更新する仕様とした(要件定義書F11の「編集して保存すると内容が更新される」に基づく解釈)。上書き確認は新規手動作成が既存の期間と重複した場合のみ発生させ、既存レコードの編集(同一レコードの更新)では発生させない
- [x] **T-068** 設定画面・AIプロバイダ/モデル/APIキー(F9)
  - DoD: 保存後APIキーが平文再表示されない
  - 注記: バックエンド(Hono `/api/settings/ai-provider`、AES-256-GCM暗号化、AIアダプタ、許可モデルリスト)はT-027〜T-030で実装済みだったため、本タスクはフロント(設定画面UI)のみを実装した。DBを直接確認し、`encrypted_api_key`が平文ではなく暗号化された値で保存されていることを手動検証済み
- [x] **T-069** 設定画面・プロンプトひな形編集(F10)
  - DoD: 編集内容が以後の生成(直接呼び出し・プロンプトコピー両方)に反映される
- [x] **T-070** 設定画面・データエクスポート(F14)
  - DoD: 個人情報を含む旨の確認画面を経てJSONがダウンロードされる
  - 注記: `export_teacher_data()` RPC(T-021で実装済み、生徒ごとにメモ・所感がまとまりAIプロバイダ設定を含まない)をそのまま呼び出すだけのため、本タスクは確認ダイアログ+ダウンロード処理のフロントのみ実装した

## 仕上げ

- [x] **T-071** 全画面のエラー表示を統一する(F13、通信/サーバーエラー時に直前の状態を維持)
  - 注記: 各画面実装タスクの過程で一貫して「try/catch + showToast、成功時のみ状態リセット(モーダルclose・入力クリア等)」のパターンを踏襲していたため、レビューの結果、追加修正は不要だった。`mutateAsync`呼び出し全箇所(30箇所)を確認し、エラー時に直前の入力内容が保持されることを確認済み
- [x] **T-072** 全画面のEmptyState表示を確認・調整する(`docs/design/screens.md` の空状態一覧に対応)
  - 注記: 実装済み全画面のEmptyStateメッセージをscreens.mdの空状態一覧と突き合わせ、文言が完全に一致していることを確認した(クラス管理・生徒名簿・科目管理・週次時間割・授業記録・生徒別メモ一覧・所感画面生成/履歴タブ)。追加修正は不要だった
- [x] **T-073** 全画面のローディング状態を確認・調整する
  - 注記: 全画面でLoadingSpinner/Skeletonによる初期読み込み表示、ボタンの`loading`状態によるミューテーション中表示が一貫して実装されていることを確認した。所感生成(直接呼び出し)はAI応答待ちが数秒〜数十秒かかる想定のため、ボタンのスピナーに加えて状況を示すラベル付きのLoadingSpinnerを追加した(`components.md`のLoadingSpinner使用箇所の指定に対応)
- [x] **T-074** アクセシビリティレビュー(コントラスト比WCAG AA、タップ領域44×44px、色以外での情報伝達)
  - 注記: (1) タップ領域: テーブル・カード内のアイコンのみボタン(編集・削除・所感アイコン等、計12箇所)とModalの閉じるボタンが実測24×24px程度だったため、44×44pxに拡大した。週次時間割の週送りボタン等、テキスト付きボタンや影響範囲の大きい要素は現状維持。(2) コントラスト比: design-system.mdの色トークンを白背景上の通常テキストとして検証したところ、`gray-500`・`success-500`・`warning-500`がWCAG AA(4.5:1)未達(3.3〜3.4:1、3:1の大文字/UI部品基準は満たす)だったため、色相を保ったまま暗く調整し4.5:1以上を確保した(`gray-700`・`gray-900`・`error-500`は当初から適合)。(3) 色以外での情報伝達: Badgeコンポーネントは元々「色のみに依存せず必ずラベルテキストを併記する」設計であり、共有区分・作成方法等はすべてテキスト併記済みであることを確認
- [x] **T-075** `shared/` 純粋関数の単体テストを整備する
  - 注記: 各画面実装タスクの進行に伴い、`shared/`配下の全ファイル(pseudonym, week, prompt-builder, resolve-weekly-slots, parse-student-rows, ai-models, schemas)に対応する`.test.ts`をその都度整備済みだったため、既に完了していた
- [x] **T-076** RPC関数8つ+RLSクロステナント拒否のDB結合テストを整備する(縮小しない対象、`docs/design.md` §7)
  - 注記: RPC関数8つは各画面実装タスクの進行に伴い既にテスト済みだったが、RLSクロステナント拒否テスト(`tests/db/rls.test.ts`)は`class`/`student`の2テーブルのみで、残り8テーブル(`subject`, `timetable_master_slot`, `weekly_subject_override`, `weekly_class_override`, `memo`, `student_comment`, `ai_provider_setting`, `prompt_template`)が未カバーだったため、本タスクで追加した。`npm run test:db`で9ファイル・35テストが全てパスすることを確認済み
- [x] **T-077** E2Eゴールデンパスを実装する(サインアップ→クラス作成→CSV登録→時間割設定→メモ記録→所感生成・保存)
  - 注記: 週次時間割画面の「この授業を記録する」から授業記録画面への遷移も経由させ、画面間連携を含めた一連の流れとして実装した(`e2e/golden-path.spec.ts`)。design.mdの方針通りAIプロバイダ呼び出しは`page.route()`でモック
- [x] **T-078** 上書き確認ダイアログ系のE2Eを2〜3本実装する
  - 注記: 所感生成タブの上書き確認(T-066a)・所感履歴タブの上書き確認(T-067)に加え、時間割マスタ設定・一括モードでの「マスごとのクラス設定を統一しますか」確認(T-058)を新規に追加し、計3本とした。この3本目の追加により、一括モード保存成功後にdraftSlotsがサーバー保存内容と同期されず、教科担任制モードに切り替えると古いクラス値が表示される状態管理バグを発見・修正した(`TimetableMasterForm.tsx`のdoSave)
- [ ] **T-079** 本番ビルド・Vercel最終デプロイを確認する
  - DoD: 本番ビルド(`npm run build`)は継続してグリーン。実デプロイの疎通確認(Vercel上でログイン・所感生成まで一通り動作すること)
  - 経緯(2026-08-17更新): 当初Cloudflare Pagesでの本番デプロイを試みたが、`npm run preview`(`opennextjs-cloudflare build`)がビルド段階で失敗するブロッカーが判明していた。原因はNext.js 16.3.1の`proxy.ts`(旧`middleware.ts`)がNode.jsランタイム専用に固定されたこと(`export const runtime = "edge"`を付与するとNext.js自身が「Proxyは常にNode.jsランタイムで動作する」とビルドエラーにする)と、`@opennextjs/cloudflare`(当時の最新1.20.2)がNode.js middlewareを明示的に拒否する(`Node.js middleware is not currently supported`)ことの組み合わせによるもので、アップストリームの既知ギャップの可能性が高く自己解決の見込みが立たなかった。ユーザーと相談の上、ホスティングをVercelに変更した(CLAUDE.md・docs/requirements.md・docs/design.md・T-005も合わせて更新済み)。VercelはNode.jsランタイムのmiddleware/proxyをネイティブサポートしているため、このブロッカーは発生しない
  - デプロイ前チェックで判明した追加の修正: Hono側の秘密鍵取得(`lib/hono-server/routes/ai.ts`の`getMasterKey()`)がCloudflare Workers専用API `getCloudflareContext()` に依存しており、Vercel上では動作しない状態だった。`process.env.ENCRYPTION_MASTER_KEY`を直接参照する実装に修正済み。Vercel側の環境変数(`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `ENCRYPTION_MASTER_KEY`)の設定、および本番用Supabaseプロジェクトの用意・マイグレーション適用はダッシュボード操作が必要なため未実施
  - 残タスク(2026-08-19解消): Cloudflare関連ファイル(`wrangler.jsonc`, `open-next.config.ts`, `lib/hono-server/cloudflare-env.secrets.d.ts`)と`package.json`のCloudflare専用scripts(`preview`/`deploy`/`upload`/`cf-typegen`)・devDependencies(`@opennextjs/cloudflare`, `wrangler`)を削除済み(`npm install`で277パッケージ削減)。ビルド・lint・vitest・E2E再確認済み
  - デプロイ環境(2026-08-19時点): GitHubリポジトリ(`numa02/my-projct260812`)は連携済み。Vercelアカウント・本番用Supabaseプロジェクトはまだ未作成で、ユーザーとダッシュボード作業を分担しながら進める

## 画面再設計

- [x] **T-080** 所感画面をクラス単位の一覧編集画面(所感管理画面)に再設計する(F9, F10, F11)
  - 背景: T-065〜T-067で実装した「クラスを選んで生徒を1人選び、タブで生成/履歴を切り替える」構成は、学期末に生徒全員分の所感をまとめて書きたいという実際の使い方に対して手数が多かった。ユーザーから「所感はクラス単位で編集していきたい。名前と所感を記載するスペースを初期表示にして、AI生成やプロンプトが必要な場合はクリックやタップで開くようにしてほしい」という要望を受け、事前に4点(画面構成/対象期間の扱い/既存詳細画面の扱い/AI生成の単位)をヒアリングした上で再設計した
  - DoD: `/comments/class/[[...id]]`でクラス+対象期間(開始日・終了日、画面全体で1つ)を指定すると、そのクラスの生徒全員分の行が一覧表示される。各行は氏名・所感入力欄(常時表示)・保存ボタンを持ち、「AIで生成する」(F9直接呼び出し/F10プロンプトコピー運用)と「過去の所感を見る」(F11履歴、対象期間以外の保存済み所感を閲覧)は折りたたみで必要な時だけ開く。生徒名簿画面の所感アイコンからは対象クラス+対象生徒のハイライト付きで遷移する。ナビゲーションメニューにも「所感管理」を追加した
  - 決定事項(ヒアリング結果): (1) 画面構成は授業記録画面(F6)と同じ折りたたみ一覧パターンを採用、ただし氏名+所感入力欄は常時表示という点が異なる。(2) 対象期間はクラス全体で1つだけ指定し、生徒ごとの個別期間指定は廃止。(3) 生徒単位の詳細画面(`/comments/students/[id]`、生成/履歴タブ)は本画面に統合し廃止。(4) AI生成は生徒ごとに個別実行(クラス一括生成は今回未実装、必要になれば別途検討)
  - 設計変更に伴う仕様変更: 対象期間が画面上部で編集前から一意に確定するため、F11の上書き確認ダイアログ(「この期間の所感は既に保存されています。上書きしますか」)が不要になり廃止した(保存操作自体が既存レコードへの意図的な更新になるため)。`docs/requirements.md` F9-F11、`docs/design/screens.md`画面12、`docs/design/user-flow.md`フロー4、`docs/design.md`(ルーティング・コンポーネント表・§5.4直接アクセスパターン例)を合わせて更新した
  - 実装: `components/comment/StudentCommentRow.tsx`(行本体、`useStudentComments()`を行ごとに呼び出し対象期間の既存所感初期表示+履歴一覧の両方に使う)、`components/comment/CommentAiAssist.tsx`(AI生成セクション、結果は保存せず行の所感入力欄にコールバックで反映するだけ)、`app/(main)/comments/class/[[...id]]/`(page.tsx, ClassCommentsView.tsx, ClassCommentsContent.tsx)。旧`app/(main)/comments/students/[id]/`一式と`CommentGenerateTab.tsx`/`CommentHistoryTab.tsx`は削除
  - テスト: `e2e/comments-class.spec.ts`(空状態・メニュー導線・一覧表示とハイライト・手動保存と復元・対象期間切替・AI直接生成・共有メモなし・プロンプトコピー運用・履歴閲覧の9ケース)を新規作成。`e2e/golden-path.spec.ts`・`e2e/settings-prompt-template.spec.ts`の所感関連ステップ、`docs/features/comments/test-cases.md`一式を新しいUIに合わせて更新
  - 残タスク: クラス単位の一括AI生成(前述の(4)で見送り)は`B-005`として後回し(MVP後)に記録した
  - **2026-08-18追記**: ユーザーから「所感のAPI生成ができない」と報告があり調査したところ、APIキー未設定のアカウントでF10(プロンプトコピー運用)が仕様通り表示されていただけでバグではなかった。ただしこれをきっかけに「AIで直接生成するかプロンプトを作成するかはAPIキー設定状況で自動的に決まるのではなく、教員に選ばせてほしい(APIキー設定済みでもプロンプトコピー運用を選べるようにしてほしい)」という要望を受けた。`CommentAiAssist.tsx`にSegmentedControl(「AIで直接生成」/「プロンプトを作成」)を追加し、既定値はAPIキー設定状況から算出しつつ教員が上書きできるようにした(`effectiveClassId`等と同じ「未選択ならstateから直接計算する」パターン)。APIキー未設定で「直接生成」を選んだ場合は設定画面へのリンク付き案内を表示し、生成不可であることを明示する。`docs/requirements.md` F9・F10、`docs/design/screens.md`画面12、`docs/design/components.md`(SegmentedControl使用箇所)を更新し、`e2e/comments-class.spec.ts`に2ケース追加

## 後回し(MVP後)

- [ ] **B-001** 生徒別メモ一覧のフィルタ機能(キーワード検索・共有区分絞り込み)── 要件定義書の未決事項
- [ ] **B-002** パスワードリセットリンク無効・期限切れ時の再申請導線の作り込み(現状は暫定フローで補完)
- [ ] **B-003** 設定画面にAIプロバイダ公式のデータ保持・学習利用ポリシーへのリンクを掲示する(`docs/design.md` §8)
- [ ] **B-004** 仮名コードの桁あふれ(1学年100クラス以上、1クラス100人以上)時の挙動定義
- [ ] **B-005** 所感管理画面でのクラス一括AI生成(現状は生徒ごとに個別実行のみ。T-080で見送り)

## 未決事項

- タスク粒度は設計時点の見積もりであり、実装時に大きすぎると判明したタスクは分割する

## バグ記録

実装完了後に見つかった実バグ(ユーザー報告・自主レビューいずれも)は `docs/bugs.md` に記録する。
